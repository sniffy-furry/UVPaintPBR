<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature android:glEsVersion="0x00030000" android:required="true" />

    <!-- API 26-29: classic runtime permission, full external storage. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />
    <!-- API 30+: "All files access" special permission, granted via a Settings
         screen rather than a runtime dialog. Needed so the in-app file picker
         can browse the real filesystem and assimp can resolve a picked
         model's sibling files (loose glTF .bin/textures, OBJ .mtl) in place,
         instead of only ever seeing the single file a SAF picker hands back. -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" tools:ignore="ScopedStorage" />

    <application
        android:allowBackup="true"
        android:extractNativeLibs="true"
        android:label="@string/app_name"
        android:theme="@style/Theme.UVPaint"
        android:requestLegacyExternalStorage="true"
        android:supportsRtl="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FilePickerActivity"
            android:exported="false"
            android:screenOrientation="landscape" />
    </application>
</manifest>
